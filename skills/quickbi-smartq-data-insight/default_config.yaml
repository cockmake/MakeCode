# Quick BI 默认配置（随技能包发布）
# 自动注册产生的 user_token 也会写入此处

# QBI 域名
server_domain: https://bi.aliyun.com

# OpenAPI 认证配置（AccessKey ID / AccessKey Secret）
api_key:
api_secret:

# Quick BI 用户 ID
user_token:

# 是否从环境变量读取认证信息
use_env_property: false

# 数据解读模型代码
model_code: SYSTEM_deepseek-v3.1
